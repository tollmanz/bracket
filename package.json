{
  "name": "bracket",
  "version": "1.0.0",
  "private": true,
  "description": "Retro static scoreboard for our annual NHL Stanley Cup playoff pick 'em, across every year we have data for.",
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "serve": "eleventy --serve",
    "verify": "node scripts/verify-data.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.5"
  }
}
